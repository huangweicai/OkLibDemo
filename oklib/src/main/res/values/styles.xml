<resources>
    <!--去掉标题栏-->
    <style name="oklib_AppTheme" parent="@style/Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--视频播放相关-->
    <style name="oklib_dialog_change_clarity" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowBackground">@color/oklib_dialog_bg</item>
    </style>

    <!--pickerview-->
    <style name="custom_dialog2" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0.3</item>
    </style>
    <style name="pickerview_dialogAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/oklib_pickerview_dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/oklib_pickerview_dialog_scale_out</item>
    </style>

    <!--dialog大全-->
    <style name="oklib_mystyle" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/oklib_dialog_enter</item>  //进入时的动画
        <item name="@android:windowExitAnimation">@anim/oklib_dialog_exit</item>    //退出时的动画
    </style>
    <style name="oklib_dialog_center" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/oklib_dialog_enter_center</item>  //进入时的动画
        <item name="@android:windowExitAnimation">@anim/oklib_dialog_exit_center</item>    //退出时的动画
    </style>
    <style name="oklib_notitle" >
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="Widget.GifMoviewView" parent="@android:style/Widget"></style>
    <style name="oklib_MyDialog" parent="AlertDialog.AppCompat.Light">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!--<item name="android:windowBackground">@drawable/hint_img</item>-->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- 地区选择dialog动画 -->
    <style name="oklib_common_single_dialog_window_anim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/oklib_region_selection_anim_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/oklib_region_selection_anim_dialog_exit</item>
    </style>

    <!-- 加载等待窗口 -->
    <style name="oklib_LoadingDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!-- popup window 显示消失动画-->
    <style name="oklib_PopWindowAnimStyle">
        <item name="android:windowShowAnimation">@anim/oklib_dialog_enter</item>
        <!-- 指定显示的动画xml  -->
        <item name="android:windowHideAnimation">@anim/oklib_dialog_exit</item>
        <!-- 指定消失的动画xml  -->
    </style>

</resources>
